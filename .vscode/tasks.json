{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "options": {
                "env": {
                    "SolutionDir": "${workspaceFolder}"
                }
            },
            "args": [
                "build",
                "${workspaceFolder}/CoreKraft.sln"
            ],
            "problemMatcher": "$tsc"
        }
    ]
}